<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.ivicel.github.githubtrends">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/CyanTheme">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.AddLanguageActivity" />
        <activity android:name=".ui.activity.RemoveLanguageActivity" />
        <activity android:name=".ui.activity.RepoPageActivity" />
        <activity android:name=".ui.activity.FavoriteLanguageActivity" />
        <activity android:name=".ui.AboutActivity"></activity>
    </application>

</manifest>